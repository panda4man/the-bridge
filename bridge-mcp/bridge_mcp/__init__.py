"""bridge-mcp: an instruction-only MCP server for The Bridge deployment API.

This package does NOT call The Bridge API. It serves authored guidance
(MCP resources + prompts) that tells an agent how to make the calls itself.
"""

__version__ = "0.1.0"
